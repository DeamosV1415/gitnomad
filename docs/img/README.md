# Screenshots

Drop these three PNGs here (exact filenames — both READMEs reference them):

| Filename | What it shows |
|----------|---------------|
| `desktop-sidebar.png` | The GitNomad panel in VS Code with synced repos |
| `desktop-shell-picker.png` | The "Which shell runs your phone commands?" picker |
| `desktop-log.png` | The GitNomad output/activity log |

These are served over `raw.githubusercontent.com`, so the VS Code Marketplace listing pulls them live from here — updating a file here updates the Marketplace image without repackaging the extension.
